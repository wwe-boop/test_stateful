---
description: Shell 脚本编写规范
globs: scripts/bash/**/*.sh,scripts/bash/*.sh
alwaysApply: false
---
# Shell 脚本规范

## 结构

- 库函数放在 `scripts/bash/lib/*.sh`，通过 `tools.sh` 聚合
- 每个 lib 文件用 `_LIB_*_LOADED` 守卫防止重复加载
- 调用方只需 `source tools.sh` 即可获得全部函数
- 入口脚本 (`autorun.sh`, `setup_env.sh`, `download_models.sh`) 通过 `REPO_ROOT` + `source tools.sh` 引导

## 现有库模块

| 模块 | 导出函数 |
|------|---------|
| `logging.sh` | `log_info`, `log_warn`, `log_error`, `log_step` + 颜色变量 `_CLR_*` |
| `utils.sh` | `retry`(指数退避), `require_cmd`, `ensure_workdir`, `check_gpu` |
| `prerequisites.sh` | `check_prerequisites`, `ensure_git_lfs`, `detect_cuda_version`, `cuda_to_torch_tag`, `check_disk_space`, `check_gpu_memory`, `check_docker` (含容器推荐) |
| `docker.sh` | `detect_driver_version`, `resolve_ngc_image`, `resolve_ngc_image_info`, `resolve_ngc_tag`, `ensure_ngc_image`, `check_docker_gpu_ready`, `print_container_recommendation`, `build_combined_triton_image`, `ensure_combined_triton_image`; NGC 兼容矩阵表 `_NGC_TRTLLM_MATRIX`; 组合镜像 `qwen3-tts-triton-base:xx.xx` (trtllm + ORT backend) |
| `venv.sh` | `ensure_venv_cmd`, `create_venv`, `ensure_venv` (mamba > conda 自动检测；缺失时交互询问安装 miniforge 或回退 venv) |
| `pip.sh` | `pip_install`, `pip_install_requirements`, `install_torch_cuda`, `install_qwen3_tts`, `install_onnx_export_deps`, `install_safetensors`, `validate_python_env` |
| `mirrors.sh` | `configure_mirrors` (检测/配置 conda+pip 国内镜像，支持交互 & `CONFIGURE_MIRRORS` env) |
| `network.sh` | `github_url`, `hf_url`, `download_hf_model`, `download_model`(auto/hf/modelscope+revision), `download_qwen3_tts_models`, `list_model_variants`, `select_model_variant`(交互式选择), `pin_model_versions`; 自动测速选择镜像(阈值 `*_SPEED_THRESHOLD_MS`)，`CONFIGURE_MIRRORS=china` 联动; 版本钉扎通过 `model_versions.conf` |
| `triton.sh` | `assemble_model_repo`, `validate_model_repo`, `build_triton_image`, `resolve_triton_deploy_image`, `triton_run`, `triton_health_check`, `triton_stop`; Phase A/B 产物到 Triton model_repository 的组装、容器生命周期管理 |
| `status.sh` | `detect_phase_a_status`, `detect_phase_b_status`, `detect_phase_c_status`, `detect_available_variants`, `print_status_summary`, `detect_resume_point`; 三阶段流水线状态检测与彩色面板输出 |

## 配置文件

| 文件 | 用途 |
|------|------|
| `model_versions.conf` | 模型版本钉扎清单 (`variant=revision`)，确保可复现性，防止上游更新破坏项目 |

## 入口脚本

| 脚本 | 用途 |
|------|------|
| `autorun.sh` | 智能入口: 串联 Phase A→B→C 全流程，支持子命令 (`all`/`setup`/`build`/`deploy`/`status`/`stop`)，无参数时交互式引导；裸传 variant 名自动走全流程 |
| `setup_env.sh` | Phase A: 构建环境 (9 步: prerequisites → submodule → workspace → mirrors → venv → deps → models → export deps → export)；MODEL_VARIANT 未指定时交互式询问用户 |
| `build_engines.sh` | Phase B: 自动检测驱动选择最佳 NGC 容器, docker run trtllm-build, 支持 `--pull-only` 预拉取 |
| `download_models.sh` | 独立下载/更新模型，支持 `--list` / `--help` / `--pin`，可脱离 autorun 单独使用 |
| `build_triton.sh` | Phase C: Triton 部署容器构建/下载/启动 (子命令: assemble / build-image / pull / run / build / stop / status)；`build-image` 构建组合镜像 (trtllm + ORT)；`run` 自动触发 `build-image`；自动组装 model_repository、启动 Triton Server |

## 编写要求

- `set -euo pipefail`
- 所有函数必须幂等（安全重复运行）
- 网络操作必须有 `retry` + 超时
- 中国网络环境：GitHub 走 `GITHUB_MIRROR`，HuggingFace 走 `HF_MIRROR`，pip/conda 走 `configure_mirrors`(BFSU)，模型下载走 ModelScope
- 用 `log_*` 函数输出，不要裸 `echo`（banner/交互提示除外）
- 新增 lib 模块时必须在 `tools.sh` 中添加 source 行
