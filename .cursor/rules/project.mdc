---
description: Qwen3-TTS Triton 项目总览 — 始终加载，提供全局上下文
alwaysApply: true
---

# Qwen3-TTS Triton 流式推理服务

## 项目目标

将阿里 Qwen3-TTS 模型拆分为 6 个独立组件，导出为 ONNX / TensorRT 引擎，部署在 Triton Inference Server 上，构建可量产的高性能流式 TTS 推理服务。

## 技术路线

- **推理引擎**: Triton Inference Server + TensorRT (/ TRT-LLM) + ONNX Runtime
- **精度**: BF16（与 FP32 同指数范围，规避 RoPE/LayerNorm/Softmax overflow）
- **核心策略**: Talker Backbone → TRT-LLM (含 KV Cache)，Code Predictor → TRT (无 KV Cache，31 步展开)，其余 → ONNX

## 模型组件 (从 Qwen3TTSForConditionalGeneration 拆分)

| 组件 | 引擎 | 调用频率 |
|------|------|---------|
| Text Embedder (~312M) | PyTorch 权重 (Orchestrator 内置) | 每请求 1 次 |
| Speaker Encoder (ECAPA-TDNN, ~6M) | ONNX | 每请求 1 次 |
| Speech Tokenizer Encoder (MimiModel, ~26M) | ONNX | 每请求 1 次 |
| **Talker Backbone** (20L Qwen3, ~180M) | **TensorRT / TRT-LLM** | 每 decode step |
| **Code Predictor** (5L Qwen3 + 31 heads, ~200M) | **TensorRT** | 每 decode step |
| Code2Wav Decoder (~60M) | ONNX | 每 chunk |

## 两阶段构建

```
构建环境 (autorun.sh):  PyTorch + qwen_tts + 模型权重 → 导出 ONNX/TRT engines
部署镜像 (Triton):      导出后的 engines + Orchestrator Python 后端 (无 PyTorch)
```

## 目录结构

```
Qwen3-TTS-Triton/
├── docs/architecture.md          ← 详细架构设计 (1600+ 行，核心参考文档)
├── scripts/
│   ├── bash/                     ← 构建环境脚本 (autorun.sh + lib/)
│   ├── export/                   ← 模型导出脚本 (TODO)
│   └── build/                    ← TRT 编译脚本 (TODO)
├── model_repository/             ← Triton 模型仓库 (TODO)
├── gateway/                      ← TTS Gateway gRPC 服务 (TODO)
├── client/                       ← 客户端示例 (TODO)
├── third_party/Qwen3-TTS/        ← 官方仓库 (git submodule)
└── workspace/models/             ← 下载的模型权重 (gitignored)
```

## 关键依赖来源

- 模型架构类 `Qwen3TTSForConditionalGeneration` 定义在 `third_party/Qwen3-TTS/qwen_tts/core/models/`（非标准 transformers，必须从子模块安装）
- 模型权重官方推荐从 **ModelScope** 下载 (`modelscope download --model Qwen/...`)
- Tokenizer: `Qwen/Qwen3-TTS-Tokenizer-12Hz` (所有模型共用)
- TTS 模型变体: Base (voice clone) / CustomVoice (9 音色) / VoiceDesign

## 性能目标

- 单步延迟: ~4.1ms (vs vllm-omni ~20ms, 5x 提升)
- GPU 利用率: >80% (vs vllm-omni 10-20%)
- 最大并发: 8-10 路 (单 GPU)
- 首包延迟 (TTS 部分): ~76ms

## 实施阶段

- Phase 1: 模型导出 + 基础验证 + CP TRT 编译风险验证
- Phase 2: TensorRT / TRT-LLM 优化
- Phase 3: 流式 + Batch (Orchestrator / Session / Flow Control)
- Phase 4: 优化 + 生产化 (CUDA Graphs, 监控, 压测)

## 约定

- 所有对话使用**中文**
- 详细技术细节参考 `docs/architecture.md`
- Shell 脚本遵循现有 `scripts/bash/lib/` 的模块化风格（幂等、带重试、支持镜像）
- Python 代码使用 BF16 精度，除非有明确理由
