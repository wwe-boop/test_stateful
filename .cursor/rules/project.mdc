---
description: Qwen3-TTS Triton 项目总览 — 始终加载，提供全局上下文
alwaysApply: true
---

# Qwen3-TTS Triton 流式推理服务

## 项目目标

将阿里 Qwen3-TTS 模型拆分为 6 个独立组件，导出为 ONNX，部署在 Triton Inference Server 上；可选 Phase B 将 ONNX 编译为 TensorRT 引擎。双后端（ONNX Runtime / TensorRT）由 config.pbtxt 与模型文件（.onnx / .plan）决定，BLS 推理逻辑一致。

## 技术路线

- **推理引擎**: Triton Inference Server + ONNX Runtime 或 TensorRT（二选一，统一 BLS）
- **精度**: BF16
- **核心策略**: 全部组件先导出 ONNX；部署时选择 engine-mode：onnx（model.onnx + onnxruntime）或 trt（model.plan + tensorrt）。Talker 拆为 talker_context（prefill + CP + codec_sum 融合）与 talker_decode_fused（decode + CP + codec_sum 融合），KV Cache 由 BLS 通过 dlpack 在 GPU 间传递。

## 模型组件 (Triton model_repository)

| 组件 | 部署形态 | 调用频率 |
|------|----------|---------|
| Text Embedder (~312M) | .pt 权重，Orchestrator 进程内 | 每请求 1 次 |
| Speaker Encoder (~6M) | ONNX/TRT 子模型 | 每请求 1 次 (voice clone) |
| Speech Tokenizer Encoder (~26M) | ONNX/TRT 子模型 | 每请求 1 次 (ICL) |
| talker_context (prefill+CP+sum) | ONNX/TRT 子模型 | 每请求 1 次 |
| talker_decode_fused (decode+CP+sum) | ONNX/TRT 子模型 | 每 decode step |
| code2wav | ONNX/TRT 子模型 | 每 chunk |
| tts_orchestrator | Python BLS | 编排上述子模型 |

## 三阶段构建

```
Phase A — export (host):  PyTorch + qwen_tts → ONNX（04 context 融合、05 decode 融合）+ .pt 权重
Phase B — build_engines.sh (可选):  trtexec 将 ONNX 编译为 .engine（所有子模型）
Phase C — build_triton.sh:  assemble（--engine-mode trt|onnx）+ run
```

- Phase A：`export_all.py` 产出 04/05 ONNX 与 06 的 .pt
- Phase B + C 共用 `tritonserver:xx.yy-py3`（trtexec 在 `/usr/src/tensorrt/bin/trtexec`）
- 单独运行导出脚本前需激活环境：`conda activate qwen3-tts`（或 venv）

## 目录结构

```
Qwen3-TTS-Triton/
├── docs/architecture.md              ← 详细架构设计 (1650+ 行，核心参考文档)
├── scripts/
│   ├── bash/
│   │   ├── autorun.sh                ← 智能入口 (串联 A→B→C, 子命令/交互, 状态面板)
│   │   ├── setup_env.sh              ← Phase A: 构建环境 (9 步, host 端)
│   │   ├── build_engines.sh          ← Phase B: trtexec 编译 ONNX→.engine（无 TRT-LLM）
│   │   ├── build_triton.sh           ← Phase C: assemble (--engine-mode onnx|trt) / pull / run / build / stop
│   │   ├── download_models.sh        ← 独立模型下载脚本
│   │   ├── export_models.sh          ← 独立模型导出脚本
│   │   ├── tools.sh                  ← lib/ 聚合入口
│   │   └── lib/                      ← 模块化函数库
│   │       ├── logging.sh            ← 彩色日志
│   │       ├── utils.sh              ← retry, require_cmd, check_gpu
│   │       ├── prerequisites.sh      ← 系统检查 (CUDA/磁盘/GPU/Docker)
│   │       ├── venv.sh               ← Python 环境 (mamba>conda>venv)
│   │       ├── pip.sh                ← pip 安装 (PyTorch/qwen-tts/onnx)
│   │       ├── mirrors.sh            ← 国内镜像配置 (TUNA)
│   │       ├── network.sh            ← 下载 (ModelScope/HF/git clone)
│   │       ├── docker.sh             ← NGC 容器自动选择 (驱动检测/兼容矩阵/镜像拉取)
│   │       ├── triton.sh             ← Triton model_repository 组装/容器管理
│   │       └── status.sh             ← 三阶段流水线状态检测
│   ├── export/                       ← 模型导出脚本
│   │   ├── export_all.py             ← 总入口 (一键导出全部组件)
│   │   ├── export_01_speech_tokenizer_encoder.py  ← Step 01 → ONNX
│   │   ├── export_02_code2wav_decoder.py          ← Step 02 → ONNX
│   │   ├── export_03_speaker_encoder.py           ← Step 03 → ONNX (base only)
│   │   ├── export_04_talker_context.py             ← Step 04 → ONNX (context 融合 CP+codec_sum)
│   │   ├── export_05_talker_decode_fused.py        ← Step 05 → ONNX (decode 融合 CP+codec_sum)
│   │   ├── export_06_embeddings.py                 ← Step 06 → .pt 权重 + config
│   │   ├── export_code_predictor.py                ← 独立 CP 调试/验证 (不在主流程)
│   │   └── utils.py                  ← 共享工具 + CodePredictorUnrolled
│   └── python/                       ← Python 工具脚本 (TODO)
├── model_repository/                  ← Triton 模型仓库 (TODO)
├── gateway/                           ← TTS Gateway gRPC 服务 (TODO)
├── client/                            ← 客户端示例 (TODO)
├── third_party/Qwen3-TTS/             ← 官方仓库 (git submodule)
└── workspace/models/                  ← 下载的模型权重 (gitignored)
```

## 关键依赖来源

- 模型架构类 `Qwen3TTSForConditionalGeneration` 定义在 `third_party/Qwen3-TTS/qwen_tts/core/models/`（非标准 transformers，必须从子模块安装）
- 模型权重官方推荐从 **ModelScope** 下载 (`modelscope download --model Qwen/...`)
- Tokenizer: `Qwen/Qwen3-TTS-Tokenizer-12Hz` (所有模型共用)
- TTS 模型变体: Base (voice clone) / CustomVoice (9 音色) / VoiceDesign
- 模型变体选项: `base-1.7b` | `custom-1.7b` | `design-1.7b` | `base-0.6b` | `custom-0.6b` | `all-1.7b` | `all`（交互式选择，非显式指定时询问用户）
- 版本钉扎: `scripts/bash/model_versions.conf` (防止上游模型更新破坏项目, `--pin` 命令锁定当前版本)

## 性能目标

- 单步延迟: ~4.1ms (vs vllm-omni ~20ms, 5x 提升)
- GPU 利用率: >80% (vs vllm-omni 10-20%)
- 最大并发: 8-10 路 (单 GPU)
- 首包延迟 (TTS 部分): ~76ms

## 实施阶段

- **Phase 1** (当前): 导出 ONNX (04a/04b 融合) + 可选 trtexec 编译 + Triton 双后端部署 + BLS 统一编排
- Phase 2: 流式 + Batch (Orchestrator / Session / Flow Control)
- Phase 3: 优化 + 生产化 (监控, 压测)

## 约定

- 所有对话使用**中文**
- 代码注释使用**英语**
- 详细技术细节参考 `docs/architecture.md`
- Shell 脚本遵循现有 `scripts/bash/lib/` 的模块化风格（幂等、带重试、支持镜像）
- Python 代码使用 BF16 精度，除非有明确理由
