# Qwen3-TTS Triton 项目阶段进展（2026-03-25）

## 1. 项目目标

当前项目的目标不是单纯把模型跑起来，而是形成一条可自动部署、可验证、可扩展的生产链路：

1. `autorun` 能根据用户设备部署当前最优实现。
2. `talker + code predictor + code2wav` 融合为单图，支持纯流式推理。
3. Triton BLS 能正确处理长文本，在接近 decode 上限时自然分段并重启 session。
4. 主链路最终支持高性能多路拼 batch。
5. 对线上场景中相对稳定的 instruct / prefix，支持 prefix KV cache，从而让主链路长期停留在高效 batched decode。

当前工作已经从“导出与部署原型”推进到“工程化验证与精度/编译问题排查”阶段。

---

## 2. 当前总体结论

截至目前，可以明确得到以下结论：

1. **BLS 不是当前随机噪声音频的主因。**
   已通过 direct backend 对比证明，`talker_code2wav_fused` 的 Triton TensorRT backend 在绕过 orchestrator/BLS 的情况下就已经与 ORT 产生数值分叉。

2. **当前线上阶段 repo 的 fused TRT 确实是 `FP16`。**
   该配置在当前 fused 图上数值不稳定，`step0` 就会严重发散，因此不能作为生产默认精度。

3. **`BF16` 明显优于 `FP16`，但仍不能保证 fused TRT 与 ORT 的逐步一致。**
   `BF16` 下 `step0` 已可对齐，但在后续 decode 中仍会出现分叉。

4. **即使使用 `FP32` TRT，fused 图也不是完全逐步等价于 ORT。**
   `FP32` 下 `step0` 几乎完全对齐，主 token 可维持更久，但 `code predictor` 的后续 code group 仍会先发生分叉，主 token 最终在后续 step 漂移。

5. **因此当前问题不是单一问题。**
   现状是两个问题叠加：
   - `FP16` 精度不足，直接不可用。
   - fused TRT 图中的 `code predictor unroll / argmax` 路径，即使在更高精度下也存在小的语义偏差。

---

## 3. 已完成工作

### 3.1 导图与图结构

已完成以下图结构改造：

1. `talker_code2wav_fused.onnx` 已扩展支持以下输入：
   - `attention_bias`
   - `past_seq_lens`
   - `cache_position`

2. `talker` 子图已在导出侧显式接受 `attention_bias + past_seq_lens`，为未来 heterogeneous past 长度拼 batch 留出了接口。

3. `code2wav` 融合路径保持流式状态输入输出，当前 code2wav KV 窗口按 72 处理，符合官方原型的 sliding window 特性。

对应文件：
- [export_09_talker_code2wav_fused.py](/home/rime/workspace/Qwen3-TTS-Triton/scripts/export/export_09_talker_code2wav_fused.py)
- [talker_unified_modules.py](/home/rime/workspace/Qwen3-TTS-Triton/scripts/export/talker_unified_modules.py)
- [generate_triton_configs.py](/home/rime/workspace/Qwen3-TTS-Triton/scripts/python/generate_triton_configs.py)
- [trt_fused_io_formats.py](/home/rime/workspace/Qwen3-TTS-Triton/scripts/python/trt_fused_io_formats.py)
- [trt_fused_talk_c2w_profiles.py](/home/rime/workspace/Qwen3-TTS-Triton/scripts/python/trt_fused_talk_c2w_profiles.py)

### 3.2 Orchestrator / BLS 侧能力

已接入以下 orchestrator 侧能力：

1. **长文本分段与 rollover**
   - 接近 decode 上限时，优先按句号、问号、顿号、换行、空白等自然边界分段。
   - 段间通过重启 session 的方式续合成，避免粗暴截断。

2. **Prefix KV cache**
   - 目前采用“尽量不改 engine 语义”的安全路线。
   - `PrefillBuilder` 会为可缓存前缀生成 key，并拆分出：
     - `cacheable_prefix_embeds`
     - `request_prefill_embeds`
   - Orchestrator 侧维护 LRU 风格的 prefix KV cache。

3. **Scheduler 基础能力与未来拼 batch 接口**
   - 已新增 padding / mask 所需的工具函数。
   - 已具备对不同 `talker_past_len` 的 padding、`attention_bias` 构造与 `past_seq_lens` 打包能力。
   - 这部分是 heterogeneous batching 的基础设施，但还不是最终稳定生产版本。

对应文件：
- [model.py](/home/rime/workspace/Qwen3-TTS-Triton/model_repository/tts_orchestrator/1/model.py)
- [prefill_builder.py](/home/rime/workspace/Qwen3-TTS-Triton/model_repository/tts_orchestrator/1/prefill_builder.py)
- [batch_decode_scheduler.py](/home/rime/workspace/Qwen3-TTS-Triton/model_repository/tts_orchestrator/1/batch_decode_scheduler.py)
- [text_segmenter.py](/home/rime/workspace/Qwen3-TTS-Triton/model_repository/tts_orchestrator/1/text_segmenter.py)

### 3.3 测试与诊断工具

已新增或强化以下验证工具：

1. **direct backend parity verifier**
   - 新增 [verify_fused_triton_backend.py](/home/rime/workspace/Qwen3-TTS-Triton/scripts/python/verify_fused_triton_backend.py)
   - 绕过 `tts_orchestrator`，直接对比：
     - 本地 ORT fused ONNX
     - Triton `talker_code2wav_fused` backend
   - 支持自动读取 Triton model config 的真实输入 dtype，避免 BF16/FP16/FP32 验证脚本本身误喂数据。

2. **TRT 精度 A/B 实验**
   - 已分别构建并验证：
     - 当前阶段 `FP16` engine
     - 手工构建 `BF16` engine
     - 手工构建 `FP32` engine

3. **CP 逐 stage 诊断**
   - 已用官方 `code predictor` 原型，对 ORT hidden 与 TRT hidden 分别做逐 stage top-k / gap 分析。
   - 这一步已证明：并非所有分叉都是“两个非常近的概率选项选错 argmax”。

---

## 4. 已验证结果

### 4.1 官方原型 / 输入格式基线

已修正官方 API 的输入格式与调用方式，官方原型音频基线已确认可正常生成完整音频，不再是只输出“你好”或错误长度的异常状态。

这一步的意义是：

1. 官方原型可作为当前所有后续验证的听感基线。
2. 之后所有 ONNX / TRT / Triton 的比对都应与这个基线链路对齐。

### 4.2 `FP16` fused TRT backend 结论

当前阶段 Triton repo 使用的是 `FP16` fused engine。

direct backend 验证结果表明：

1. `step0 prefill` 就已经出现严重分叉。
2. `wav_cos` 接近 0，生成结果会退化为随机噪声级别。

结论：

- `FP16` 在当前 fused 图上不可接受。
- 当前生产主线不应继续以 `FP16` 作为默认推理精度。

### 4.3 `BF16` fused TRT backend 结论

手工构建 BF16 fused engine 后，direct backend 验证结果表明：

1. `step0` 已可与 ORT 对齐。
2. `step1` 主 token 仍可一致。
3. 但 `code predictor` 的后续 token 已开始分叉。
4. `step2` 起主链路开始明显漂移。

同时，逐 stage CP 诊断显示：

1. 有些 stage 确实属于接近并列时的 argmax 摇摆。
2. 但也存在 gap 明显不小却选出完全不同 token 的情况。

结论：

- `BF16` 比 `FP16` 好很多，是更合理的候选精度。
- 但 `BF16` 仍不足以证明 fused TRT 已达到与 ORT 语义等价。

### 4.4 `FP32` fused TRT backend 结论

已手工编译 `FP32` fused TRT engine，并单独组装 direct Triton repo 做验证。

结果：

1. `step0` 几乎完全对齐 ORT。
   - `hidden_cos`、`logits_cos`、`codec_sum_cos` 约等于 1
   - `wav_cos = 0.999545`

2. `step1`、`step2` 主 token 仍可保持一致。

3. 但 `code predictor` 的后续 code group 已在更早阶段开始分叉。

4. 扩展到更多 step 后，主 token 会在后续 step 漂移。

结论：

- `code2wav` 在 `FP32` 下不是当前首要问题。
- 更核心的问题已经收缩到 `fused TRT` 中的 `code predictor unroll / argmax / codec_sum` 路径。

### 4.5 BLS 责任边界结论

当前已经可以明确：

1. **随机噪声问题不是 BLS 脚本本身导致的。**
2. 当前最主要的问题在 engine/backend 层，而不是 orchestrator 的输入拼装。

这意味着当前排障优先级应当是：

1. 先把 fused TRT backend 的数值/语义问题钉死。
2. 再继续推进 BLS 侧 continuous batching、prefix cache 调度与长文本编排。

---

## 5. 当前代码状态判定

为避免误读，当前代码按“已验证 / 已接入未闭环 / 未完成”三类划分如下。

### 5.1 已验证可用

1. 官方 API 基线调用修正。
2. fused ONNX -> Triton backend 的 direct parity 工具链。
3. `FP16/BF16/FP32` 三种 fused TRT direct backend 的数值对比能力。
4. `FP32` fused TRT 的 `step0` 高度对齐。
5. Orchestrator 的长文本分段逻辑本身与 prefix cache 基础逻辑已落代码。

### 5.2 已接入，但尚未完成全链闭环

1. `attention_bias + past_seq_lens` 导图接口
2. Scheduler 基础设施
3. Prefix KV cache 跨请求复用
4. 长文本 segment rollover 与 session 重启
5. Orchestrator 中利用 padding/mask 做 heterogeneous batched decode 的代码路径

这些部分已经在代码中接好接口，但因为当前 fused TRT backend 还没有达到稳定、可信的数值行为，所以不能认定为“生产可用”。

### 5.3 明确尚未完成

1. ONNX -> TRT -> Triton 的全链路高保真闭环
2. fused TRT 在 `BF16` 或更优混合精度下的稳定方案
3. 真正可上线的高性能 continuous batching
4. autorun 一键部署最佳配置
5. 基于设备能力自动选择精度 / engine / repo 组装策略

---

## 6. 当前主要问题清单

### 问题 1：`FP16` fused TRT 不可用

表现：

1. `step0` 即严重发散
2. `wav` 直接失真
3. 输出呈随机噪声

结论：

- 这不是可通过 BLS 修补的问题。

### 问题 2：`BF16` 仍会在 decode 过程中发生 CP 语义分叉

表现：

1. `step0` 对齐
2. `step1` 主 token 一致，但部分 CP group 已漂
3. 后续 `codec_sum` 被带偏，导致主链路进一步发散

### 问题 3：即使 `FP32`，fused TRT 仍非严格等价于 ORT

表现：

1. `step0` 几乎完美
2. 但 CP 后续 group 仍出现差异
3. 主 token 延后发散

这说明当前剩余问题更偏向：

1. TensorRT 对 fused 图中特定子路径的 lowering 语义差异
2. 或者当前 fused 图结构过于敏感，不适合把 CP unroll 与 argmax 全部压在同一 TRT 子图内

### 问题 4：当前 build 脚本的 fused precision 选择存在认知陷阱

已发现：

1. fused build 路径的实际精度与 I/O format 主要由 `triton_manifest.json` 驱动。
2. 单纯传 `ENGINE_DTYPE=bf16` 并不会可靠覆盖 fused build 配置。

这会导致用户误以为自己在编 `BF16`，但实际 repo/engine 仍是 `FP16`。

---

## 7. 待办事项

### 7.1 第一优先级

1. **把问题继续收缩到 `code_predictor_unrolled` 子图**
   - 做 standalone `code_predictor_unrolled.onnx`
   - ORT vs TRT(FP32/BF16) 对照
   - 固定 hidden 输入，排除 talker / code2wav 干扰

2. **明确 fused TRT 的可用精度策略**
   - 如果 `CP` 子图在 TRT 上无法可靠保持语义，则需要重新决定：
     - 是否让 talker / CP / code2wav 继续单图融合
     - 是否采用分图精度分层

3. **修正 build / assemble / autorun 的精度传递逻辑**
   - 避免 manifest、env、Triton config 三者不一致

### 7.2 第二优先级

1. 在 engine 方案稳定后，重新做完整全链验证：
   - 官方 API
   - fused ONNX
   - TRT engine
   - Triton direct backend
   - Triton orchestrator
   - 音频听感对比与保存

2. 完成长文本 rollover 的全链验证
   - 包括句读切分
   - session 重启
   - 段间音频连续性

3. 完成 prefix KV cache 的收益验证
   - 命中率
   - 首包延迟改善
   - 对 batch decode 吞吐的提升

### 7.3 第三优先级

1. 真正上线 continuous batching
2. 完成 autorun 一键部署最佳配置
3. 为不同 GPU 自动选择：
   - `fp32 / bf16 / fp16`
   - Triton repo 布局
   - engine profile

---

## 8. 当前推荐主线

从当前证据看，推荐主线如下：

1. **停止以 `FP16 fused TRT` 为目标继续推进。**
2. **把 `BF16` 视为更合理的默认候选，但不直接宣布其已可上线。**
3. **优先拆解 `code_predictor_unrolled` 的 TRT 语义偏差。**
4. **在 engine/backend 彻底稳定前，不继续扩大 orchestrator/scheduler 的复杂度。**
5. **长文本分段、prefix cache、scheduler 相关代码继续保留，但视为“待 engine 稳定后再做收益化”的第二层工作。**

---

## 9. 当前进度评估

如果把项目按 5 个阶段粗分：

1. **阶段 A：模型拆分与导图**
   - 进度：**80%**

2. **阶段 B：TRT 编译与 Triton 组装**
   - 进度：**70%**

3. **阶段 C：数值一致性与音频可用性**
   - 进度：**55%**

4. **阶段 D：生产特性（长文本、prefix cache、scheduler）**
   - 进度：**50%**

5. **阶段 E：一键部署 autorun**
   - 进度：**20%**

整体阶段判断：

- **项目已进入“关键收敛阶段”，而不是“从零开始阶段”。**
- 最大阻塞点已经从“能不能导图 / 能不能编译”转为“fused TRT 语义是否足够可信，能否作为生产主链路”。**

---

## 10. 本文档对应的关键文件

核心实现：
- [model.py](/home/rime/workspace/Qwen3-TTS-Triton/model_repository/tts_orchestrator/1/model.py)
- [prefill_builder.py](/home/rime/workspace/Qwen3-TTS-Triton/model_repository/tts_orchestrator/1/prefill_builder.py)
- [batch_decode_scheduler.py](/home/rime/workspace/Qwen3-TTS-Triton/model_repository/tts_orchestrator/1/batch_decode_scheduler.py)
- [text_segmenter.py](/home/rime/workspace/Qwen3-TTS-Triton/model_repository/tts_orchestrator/1/text_segmenter.py)

导出与 TRT 组装：
- [export_09_talker_code2wav_fused.py](/home/rime/workspace/Qwen3-TTS-Triton/scripts/export/export_09_talker_code2wav_fused.py)
- [talker_unified_modules.py](/home/rime/workspace/Qwen3-TTS-Triton/scripts/export/talker_unified_modules.py)
- [generate_triton_configs.py](/home/rime/workspace/Qwen3-TTS-Triton/scripts/python/generate_triton_configs.py)
- [trt_fused_io_formats.py](/home/rime/workspace/Qwen3-TTS-Triton/scripts/python/trt_fused_io_formats.py)
- [trt_fused_talk_c2w_profiles.py](/home/rime/workspace/Qwen3-TTS-Triton/scripts/python/trt_fused_talk_c2w_profiles.py)

验证：
- [verify_fused_triton_backend.py](/home/rime/workspace/Qwen3-TTS-Triton/scripts/python/verify_fused_triton_backend.py)

