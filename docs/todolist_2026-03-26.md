# TODO (2026-03-26)

## 问题清单

- [ ] `talker` 与 `code2wav` 的 KV cache 语义不一致（padding+mask 策略未统一）。
- [ ] `code2wav` 缺少与 `talker` 对齐的动态 past（物理长度与逻辑长度分离）能力，异构 past 拼 batch 不稳定。
- [ ] `code2wav` 冷启动未统一使用 dummy KV（物理 `past_len=1`）策略，空 tensor 经过 DLPack 可能报错。
- [ ] `code2wav` 的 sliding-window(72) 与外部 mask 约束未形成单一、可维护的接口。
- [ ] 导出/构建/调度三处输入签名与 profile 需要完全一致，否则会出现“能导出但运行或构建不稳”。
- [ ] TensorRT 构建存在 BF16 clipping 警告：`contains out-of-range weights when cast to BFloat16`，需定位具体常量来源并结构化消除。
- [ ] 代码冗余：`talker` 和 `code2wav` 类似逻辑分叉，维护成本高，容易回归。

## 推进规则（强制）

- [ ] 每次只做一组最小改动（一个明确目标）。
- [ ] 每次改动后立刻执行：
  1. 导出：`export_09_talker_code2wav_fused.py`
  2. 构建：`build_engines.sh --variant custom-1.7b --max-batch-size 8`
- [ ] 每一步记录“改动点 -> 构建结果 -> 新日志关键字（Myelin/Warning）”。
- [ ] 若某一步引入新 Myelin 报错，立即回滚该步并缩小改动范围重试。

## 当前执行顺序

1. [x] 统一 `talker/c2w` KV cache 的抽象与 I/O 约定（先定接口，后改实现）。
2. [x] 在 `code2wav` 落地 padding+mask（外部输入 mask，支持拼 batch）。
3. [x] 在 `code2wav` 落地 dummy cold-start（物理 `past_len=1`，逻辑 `past=0`）。
4. [ ] 对齐导出/构建/profile/调度四处签名。
5. [ ] 定位并消除 BF16 clipping 常量来源。
6. [ ] 一致性检查与回归验证（拼 batch、prefill、decode、engine build）。

## 逐步验证记录

- Step A: 接入 `c2w_attention_bias`（`code2wav_streaming` + `export_09` + profile/io-format）
  - 结果: `export_09` 通过；`build_engines` 通过（engine 生成成功）。
  - 现象: 仍存在 BF16 clipping warning（未新增 Myelin 报错）。
- Step B: 冷启动 dummy past 长度统一为 `1`（`code2wav` state 默认 + profile min）
  - 结果: `export_09` 通过；`build_engines` 通过（engine 生成成功）。
  - 现象: BF16 clipping warning 仍在；未出现新的 Myelin 报错。
- Step C: 去掉 `SlidingWindowKVCache.update` 的 `past_len==0` 分支（统一 `cat + truncate`）
  - 结果: `export_09` 通过；`build_engines` 通过（engine 生成成功）。
  - 现象: BF16 clipping warning 仍在；未出现新的 Myelin 报错。
- Step D: 对齐 `export_06`（`c2w_attention_bias` 外部输入 + dummy `past_len=1`）
  - 结果: `export_06_code2wav_decoder` 导出与 ONNX 校验通过。
  - 现象: 与 fused 路径输入语义保持一致。
- Step E: 统一 `talker/c2w` causal mask 写法（`(col>row).to(dtype) * -1e4`，去掉 `torch.tensor` 常量分支）
  - 结果: `export_09` 通过；`build_engines` 通过（engine 生成成功）。
  - 现象: BF16 clipping warning 仍为 16 条；未出现新的 Myelin 报错。
- Step F: `out-of-range` warning 定位（`trtexec --verbose`）
  - 结果: warning 对应 `Unnamed Layer* 88657/88815...89027`，集中出现在 `code2wav.decoder.quantizer` 常量区（`rvq_first/rvq_rest` 周边）。
  - 结论: 当前 warning 与 attention mask 无关；下一步需要改 `tokenizer_12hz` quantizer 结构，消除该分支导出的异常常量路径。
- Step G: 量化器导出结构改造（仅 `scripts/export` 本地补丁，不改 `third_party`）
  - 改动: 在 `code2wav_streaming.py` 中预先物化 codebook `inference_embedding`，并对 codebook 实例做本地 `decode` 绑定，导出时直接 `F.embedding`，绕开 `cluster_usage/embedding_sum` 的导图计算链。
  - 结果: `export_09` 通过；`build_engines` 通过（engine 生成成功）。
  - 现象: BF16 clipping warning 从 16 条降为 0；未出现 Myelin 报错。
- Step H: 一致性复核（`export_06`）
  - 结果: `export_06_code2wav_decoder.py --device cuda --dtype bf16` 导出 + ONNX 校验通过。
  - 现象: 本地 quantizer 导出补丁未破坏 code2wav 单模型导出路径。
- Step I: `streaming_causal_transconv` 结构改写（去掉 `clone + in-place slice`，改为函数式 `cat`）
  - 改动: `raw[..., :rp] + overlap_state` 与 `raw[..., rp:-rp]` 组装输出，避免 in-place 写回。
  - 结果: `export_09` 通过；`build_engines` 通过（engine 生成成功）。
  - 现象: 基线可编译性保持不变；说明该结构本身不是当前 Myelin 的唯一触发点。
- Step J: c2w conv/transconv state 动态轴隔离
  - 改动-1: 给 `c2w_conv_state_* / c2w_transconv_overlap_*` 的输入+输出都加 batch 动态轴。
  - 结果-1: `export_09` 通过；`build_engines` 失败，复现 `MyelinCheckException tensor.cpp:852`。
  - 改动-2: 仅保留输入 batch 动态轴（去掉输出动态轴）。
  - 结果-2: 仍失败，复现同一 Myelin 错误。
  - 结论: 当前触发点在“这组 state 的输入 batch 动态化”本身，而非输出动态轴。
- Step K: 回退到可编译基线（仅 c2w `past_kv` 保持动态，conv/transconv state 不动态）
  - 结果: `export_09` 通过；`build_engines` 通过（engine 生成成功，未见 Myelin/out-of-range）。
- Step L: “碎算子实现细节”隔离（`Conv1dInsertZeros` 去除 in-place scatter）
  - 改动: `up[..., 0::stride] = x` 改为 `reshape + cat + slice`（纯函数式）。
  - 数值: 对 4 组 transconv 参数与 `ConvTranspose1d` 对齐，最大误差约 `1e-6`。
  - 动态轴验证: 重新开启 `c2w_conv/transconv state` 输入 batch 动态后，`build_engines` 仍复现同一 Myelin 错误。
  - 结论: Myelin 触发不由该 in-place scatter 引入；问题仍在“state 输入 batch 动态化 + 当前 fused 图”组合。
- Step M: 再次回退到可编译基线
  - 结果: `export_09` 通过；`build_engines` 通过（engine 生成成功）。
- Step N: `build_engines.sh` 与 `export_06` 输入签名对齐修复
  - 问题: `code2wav_decoder` 构建脚本缺少 `c2w_attention_bias`（`inputIOFormats` 数量不匹配），且 min past 仍为 `0`。
  - 改动: 在 `build_peripheral_engines` 的 code2wav 分支补齐 `c2w_attention_bias` 的 shapes 与 IO format，并将 `past_kv` min 改为 `1`（对齐 dummy cold-start）。
  - 结果: 消除了“inputIOFormats 数量不匹配”错误，进入真实编译阶段。
- Step O: 在 `export_06`（单 code2wav）复现 Myelin
  - 配置: conv/transconv state 输入 batch 动态 + `BUILD_VERIFICATION_ENGINES=1`。
  - 结果: `code2wav_decoder` 构建失败，复现
    `MyelinCheckException: tensor.cpp:852 (LHS 4, RHS 3)`，
    ForeignNode 落在 `/layers.7/self_attn/Slice_6.../Clip`。
  - 结论: 该问题可在 `06` 独立复现，已排除 fused-only 干扰。
- Step P: 触发边界确认（`06` 关闭 conv/transconv state batch 动态）
  - 配置: 仅 `past_kv` 保持动态，conv/transconv state 不设动态轴。
  - 结果: `code2wav_decoder` 构建通过。
  - 结论: 触发条件与“conv/transconv state batch 动态化”强相关。
- Step Q: 变体尝试（conv/transconv state 使用独立动态符号）
  - 配置: 将 conv/transconv state 的 batch 轴改成独立符号（不与 `codes` 共用 `batch`）。
  - 结果: 仍复现同一 Myelin 错误。
  - 结论: 问题不由动态符号命名耦合导致。
- Step R: 变体尝试（仅 code2wav transformer 局部 FP32）
  - 配置: 在 `code2wav_streaming` 中将 pre_transformer 前后显式转为 FP32，再保留 conv/transconv state batch 动态。
  - 结果: `code2wav_decoder` 仍复现同一 Myelin（`/layers.7/self_attn/Slice_6.../Clip`）。
  - 结论: 问题不由 BF16 transformer 计算精度单独触发；局部 FP32 不能规避该 Myelin。
- Step S: 回退并确认当前稳定基线
  - 回退: 撤销 Step R；`export_06` 恢复为“仅 `past_kv` 动态，conv/transconv state 非动态”。
  - 结果: `export_06` 通过；`BUILD_VERIFICATION_ENGINES=1` 下 `code2wav_decoder` 构建通过。
  - 当前可用结论: 单模型 `06` 已稳定复现/隔离问题边界，且基线可编译。
- Step T: 基于日志的等价替换尝试（去掉 attention 内部 mask Slice）
  - 改动: 在 `scripts/export/code2wav_streaming.py` 本地 monkey-patch `eager_attention_forward`，将
    `attention_mask[:, :, :, :key_len]` 改为直接使用外部对齐好的 `attention_mask`。
  - 同步: 在 `export_06` 重新开启 `conv_state_* / transconv_overlap_*` 的输入输出 batch 动态轴。
  - 结果: `export_06` 导出通过；`build_engines` 仍失败，Myelin 依旧触发。
  - 新现象: `ForeignNode` 从 `/layers.7/self_attn/Slice_6.../Clip` 迁移为 `/Slice_1.../Clip`，
    说明 attention 的该切片是触发链路一部分，但不是唯一根因。
- Step U: 继续前移日志起点（quantizer 切片改固定 Split）
  - 改动: 在 `code2wav_streaming` 本地补丁中将 quantizer 的
    `codes[:, :n_q_semantic] / codes[:, n_q_semantic:]` 改为 `torch.split(codes, [1, 15], dim=1)`。
  - 结果: `export_06` 导出通过；`build_engines` 仍失败，Myelin 依旧触发。
  - 新现象: `ForeignNode` 进一步变为 `/Split_7.../Clip`。
  - 结论: 仅替换前端 Slice/ Split 不足以消除问题，需要继续做“分段断图”定位真正触发块。
- Step V: 分段定位（绕过 quantizer，仅保留 transformer+decoder）
  - 配置: `C2W_DEBUG_BYPASS_QUANTIZER=1`，其余路径保持不变。
  - 结果: `build_engines` 仍复现 Myelin（`tensor.cpp:852`）。
  - 新现象: `ForeignNode` 变为 `/rotary_emb/Unsqueeze_3.../Clip`，说明 quantizer 不是根因。
- Step W: 分段定位（绕过 quantizer + transformer，仅保留 decoder 主干）
  - 配置: `C2W_DEBUG_BYPASS_QUANTIZER=1` + `C2W_DEBUG_BYPASS_TRANSFORMER=1`，并显式保活 `cache_position/c2w_attention_bias` 输入签名。
  - 结果: `code2wav_decoder` 在动态 conv/trans state 条件下可成功编译并通过 `trtexec`。
  - 结论: 根因位于 transformer 子图（含 RoPE/attention 相关链路），decoder conv/transconv 主干本身可编译。
- Step X: 结构等价替换尝试（RoPE reshape 化 + transformer->decoder 显式边界）
  - 改动: RoPE `unsqueeze` 链替换为 `reshape+broadcast`；在 transformer 输出后增加 `contiguous().clone()` 边界。
  - 结果: 仍复现同一 Myelin，`ForeignNode` 仍为 `/Split_7.../Clip`。
  - 结论: 当前级别的局部结构替换不足以规避 Myelin；需要更强的“子图级断开/分引擎”或 TRT 侧修复。
- Step Y: transformer-only 最小图验证（证伪“TRT 不支持 transformer”）
  - 改动: 导出 `/tmp/c2w_transformer_only.onnx`（仅 pre_transformer，保留动态 `past_kv` + 外部 `c2w_attention_bias`）。
  - 构建: 独立 `trtexec`（TRT 10.15.1, BF16, batch/past 动态 profile）。
  - 结果: 编译与序列化通过（`PASSED`，engine 已保存）。
  - 结论: TRT 支持该 transformer；问题是“与当前 code2wav 其余子图合并后的特定图形态”触发 Myelin，而非 transformer 算子本身不支持。
- Step Z: `Split -> Slice` 尝试（quantizer 路径去 Split）
  - 改动: 本地补丁将 quantizer/RVQ decode 改为 `narrow/index`，避免 `torch.split`/`enumerate(codes)` 导出 Split。
  - 结果: ONNX 中 `Split` 节点数量降为 `0`，但动态 state 构建仍复现 Myelin。
  - 现象: ForeignNode 回到 `/layers.7/self_attn/Slice_6.../Clip`。
  - 结论: 仅消除 quantizer Split 不足以解决问题，transformer 子图仍是主触发区。
- Step AA: 组合 rank consistency 尝试（no-split quantizer + attention mask 去 Slice + RoPE reshape）
  - 结果: 构建仍失败，复现同一 Myelin；ForeignNode 迁移为 `/Slice_1.../Clip`（或回到 `Slice_6`）。
  - 结论: 当前级别的等价替换只能“迁移触发点”，尚未消除 Myelin。
- Step AB: transformer->decoder 桥接强边界（FP32 cast bridge）
  - 改动: 在桥接处加入 `to(fp32)->to(dtype)`（调试开关）。
  - 结果: 构建仍失败，ForeignNode 回到 `/layers.7/self_attn/Slice_6.../Clip`。
  - 结论: Cast 边界不足以打断 Myelin 失败链。
- Step AC: `SnakeBeta/bridge` 连续结构规避复测（`06` 最小图）
  - 改动: 先后尝试 `SnakeBeta reshape 化`、transformer->decoder bridge 改成 `ReduceSum` / `depthwise 1x1 Conv` / `CumSum`。
  - 结果: 这些边界节点都能成功进 ONNX，但 `code2wav_decoder` 仍复现同一 Myelin；ForeignNode 依次仍可吞掉 bridge，失败形态保持在 `/Slice_1.../Clip`。
  - 结论: 仅靠“桥接恒等算子换形态”不足以解决问题，必须直接改 state 的 batch 处理方式。
- Step AD: `conv/transconv state` 固定 `max_batch` + 图内局部 pad/slice（当前有效规避）
  - 改动:
    1. 新增 `C2W_STATIC_STATE_BATCH` 导出/构建开关。
    2. 仅将 `conv_state_* / transconv_overlap_*` 输入导出为固定 batch（例如 `8`）；`past_kv_*` 仍保留真实动态 batch。
    3. 将 state 的 batch 对齐从“forward 开头统一 `state[:B]`”下沉到局部 streaming conv/transconv 使用点，避免图头出现一串 `Slice`。
    4. 在 transformer 输出后，将 vocoder hidden pad 到固定 state batch，再跑 upsample + decoder；最终仅对 `wav` 切回真实 batch。
  - 验证-1:
    - 命令: `C2W_STATIC_STATE_BATCH=8 python scripts/export/export_06_code2wav_decoder.py --device cpu`
    - 结果: 导出与 ONNX 校验通过。
    - 构建: 手工 `trtexec`（conv/trans states min=opt=max=8，其余 batch 仍动态）通过，`PASSED`。
  - 验证-2:
    - 命令: `C2W_STATIC_STATE_BATCH=8 python scripts/export/export_09_talker_code2wav_fused.py --variant custom-1.7b --device cpu`
    - 结果: fused ONNX 两轮校验通过。
    - 构建: `C2W_STATIC_STATE_BATCH=8 bash scripts/bash/build_engines.sh --variant custom-1.7b --max-batch-size 8`
    - 结果: `talker_code2wav_fused.engine` 构建成功。
  - 结论:
    - 当前可用解法不是“保持 conv/trans state 输入 batch 完全动态”，而是“将这组 state 固定为 `max_batch`，在图内对 decoder 主干做 batch pad，对外仍保留主图动态 batch”。
    - 这条路径已经规避 `MyelinCheckException tensor.cpp:852`，并保住 fused 图的动态 batch 能力。
